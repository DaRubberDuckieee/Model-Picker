---
allowed-tools: Bash(npm:*), Bash(echo:*)
description: Build the project
---

Build the project using the appropriate build command.

Current directory status: !`ls -la`

Check for package.json and run the build script.
